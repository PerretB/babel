(function() {

      var dependencies = [
            "babel.exercice"
      ];

      var module = angular.module("babel", dependencies);

      module.controller("AppController", [
            "$scope",
            function($scope) {
                  
            }
      ]);

})();
